import photoLaia from "../images/portfolioImgWithBckgr.png";
// import "../styles/layouts/PortfolioList.scss";
const PortfolioList = (props) => {
  const oneProject = props.map((project) => {
    return;
  });
};

export default PortfolioList;
